declare namespace BaseEntity {
 export interface ListBaseEntity {
  msg: string;
  code: string;
  totalNum: number;
  totalPage: number;
 }
 export interface BaseEntityResult {
  msg: string;
  code: string;
 }
}
